<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0, user-scalable=yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <meta name="theme-color" content="#5865f2">
    <title>QuestCord - Quest Across Discord</title>
    <meta name="description" content="Complete quests, defeat bosses, and climb the global leaderboard in this immersive Discord RPG experience">
    <link rel="icon" type="image/x-icon" href="/images/favicon.ico">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@700;900&family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <!-- Scroll Path Line (disabled) -->
    <!-- <div class="scroll-path-container" id="scrollPathContainer"></div> -->

    <div class="hero">
        <div class="container">
            <div class="hero-title-container">
                <img src="/images/compass.png" alt="Compass" class="hero-icon" onerror="this.style.display='none'">
                <h1 class="hero-title">QuestCord</h1>
            </div>
            <p class="hero-subtitle">Quest across the Discord universe</p>
            <p class="hero-description">Complete quests, defeat bosses, and climb the global leaderboard in this immersive Discord RPG experience</p>
            <div class="hero-buttons">
                <a href="https://discord.com/oauth2/authorize?client_id=<%= process.env.DISCORD_CLIENT_ID %>&permissions=2048&scope=bot%20applications.commands" class="btn btn-primary" target="_blank">Add to Discord</a>
                <a href="#about" class="btn btn-secondary">Learn More</a>
            </div>
        </div>
    </div>

    <div class="stats-section">
        <div class="container">
            <div class="stats-grid">
                <div class="stat-card">
                    <div class="stat-value"><%= stats.total_servers.toLocaleString() %></div>
                    <div class="stat-label">Servers</div>
                </div>
                <div class="stat-card">
                    <div class="stat-value"><%= stats.total_users.toLocaleString() %></div>
                    <div class="stat-label">Users</div>
                </div>
                <div class="stat-card">
                    <div class="stat-value"><%= stats.total_quests_completed.toLocaleString() %></div>
                    <div class="stat-label">Quests Completed</div>
                </div>
                <div class="stat-card">
                    <div class="stat-value"><%= stats.total_currency_formatted %></div>
                    <div class="stat-label">Dakari in Circulation</div>
                </div>
                <div class="stat-card">
                    <div class="stat-value"><%= stats.total_gems_formatted %></div>
                    <div class="stat-label">Gems in Circulation</div>
                </div>
            </div>
        </div>
    </div>

    <div id="about" class="about-section">
        <div class="container">
            <div class="content-with-image">
                <div class="content-text">
                    <h2 class="section-title">What is QuestCord?</h2>
                    <p class="section-description">QuestCord transforms Discord servers into a virtual world where you can complete quests, battle bosses, and earn rewards. Each server becomes a unique location with daily rotating quests.</p>

                    <div class="features-grid">
                        <div class="feature-card">
                            <h3>Daily Quests</h3>
                            <p>Complete 5 unique quests per server each day. Travel between servers to continue your adventure.</p>
                        </div>
                        <div class="feature-card">
                            <h3>Boss Battles</h3>
                            <p>Bosses randomly spawn across servers. Team up with other players to defeat them and earn massive rewards.</p>
                        </div>
                        <div class="feature-card">
                            <h3>Global Leaderboard</h3>
                            <p>Compete with players worldwide. Top 3 players each month receive exclusive rewards.</p>
                        </div>
                        <div class="feature-card">
                            <h3>Economy System</h3>
                            <p>Earn Dakari and gems from quests and bosses. Use them to purchase rare items and upgrades.</p>
                        </div>
                    </div>
                </div>
                <div class="content-image">
                    <img src="/images/compass.png" alt="Quest Example" onerror="this.src='https://via.placeholder.com/500x600/7c3aed/ffffff?text=Quest+System'">
                </div>
            </div>
        </div>
    </div>

    <div class="activity-section">
        <div class="container">
            <div class="content-with-image reverse">
                <div class="content-image">
                    <img src="/images/notification.png" alt="Live Activity" onerror="this.src='https://via.placeholder.com/500x600/5865f2/ffffff?text=Live+Activity'">
                </div>
                <div class="content-text">
                    <h2 class="section-title">Live Activity</h2>
                    <p class="section-description">See what's happening across the QuestCord universe in real-time. Watch as players complete quests, defeat bosses, and climb the ranks!</p>
                    <div class="activity-container" id="activity-feed">
                        <div class="activity-item">Loading activity...</div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="leaderboard-section">
        <div class="container">
            <div class="content-with-image">
                <div class="content-text">
                    <h2 class="section-title">Global Leaderboard</h2>
                    <p class="section-description">Compete with players from around the world! Top 3 players each month receive exclusive rewards and eternal glory.</p>
                    <div class="leaderboard-container">
                        <% if (topPlayers.length === 0) { %>
                            <p class="empty-state">No rankings yet. Be the first to compete!</p>
                        <% } else { %>
                            <div class="leaderboard-top-three">
                                <% topPlayers.slice(0, 3).forEach((player, index) => { %>
                                    <div class="leaderboard-item rank-<%= index + 1 %>">
                                        <div class="rank-badge">
                                            <% if (index === 0) { %>🥇
                                            <% } else if (index === 1) { %>🥈
                                            <% } else if (index === 2) { %>🥉
                                            <% } %>
                                        </div>
                                        <div class="player-info">
                                            <div class="player-name"><%= player.username %></div>
                                            <div class="player-score"><%= player.score.toLocaleString() %> pts</div>
                                        </div>
                                    </div>
                                <% }); %>
                            </div>
                        <% } %>
                    </div>
                </div>
                <div class="content-image">
                    <img src="/images/leaderboard.png" alt="Leaderboard" onerror="this.src='https://via.placeholder.com/500x600/fbbf24/000000?text=Leaderboard'">
                </div>
            </div>
        </div>
    </div>

    <div class="faq-section">
        <div class="container">
            <h2 class="section-title">Frequently Asked Questions</h2>
            <div class="faq-grid">
                <div class="faq-item">
                    <h3>How do I get started?</h3>
                    <p>Add QuestCord to your Discord server, then use /tutorial to learn the basics. Start with /quests to see available quests.</p>
                </div>
                <div class="faq-item">
                    <h3>When do quests reset?</h3>
                    <p>Quests rotate daily at midnight NZ time. Each server gets 5 unique quests per day.</p>
                </div>
                <div class="faq-item">
                    <h3>How do boss spawns work?</h3>
                    <p>Bosses spawn randomly in opted-in servers. Only one boss can be active at a time, and it stays for 60 minutes or until defeated.</p>
                </div>
                <div class="faq-item">
                    <h3>What are the leaderboard rewards?</h3>
                    <p>Top 3 players each month receive Dakari and gems. The leaderboard resets on the 1st of each month.</p>
                </div>
                <div class="faq-item">
                    <h3>Can I opt out of the quest system?</h3>
                    <p>Yes, server owners can use /optout to disable quests in their server. You can re-enable it anytime with /optin.</p>
                </div>
                <div class="faq-item">
                    <h3>How do I earn Dakari and gems?</h3>
                    <p>Complete quests and defeat bosses to earn rewards. Higher difficulty quests and bosses give better rewards.</p>
                </div>
            </div>
        </div>
    </div>

    <% if (staff.length > 0) { %>
    <div class="staff-section">
        <div class="container">
            <h2 class="section-title">Team</h2>
            <div class="staff-grid">
                <% staff.forEach(member => { %>
                    <div class="staff-card">
                        <% if (member.avatar_url) { %>
                            <img src="<%= member.avatar_url %>" alt="<%= member.username %>" class="staff-avatar">
                        <% } %>
                        <div class="staff-name"><%= member.username %></div>
                        <div class="staff-role <%= member.role.toLowerCase() %>"><%= member.role %></div>
                    </div>
                <% }); %>
            </div>
        </div>
    </div>
    <% } %>

    <% if (peasants && peasants.length > 0) { %>
    <div class="staff-section">
        <div class="container">
            <h2 class="section-title">Peasants</h2>
            <div class="staff-grid">
                <% peasants.forEach(member => { %>
                    <div class="staff-card <%= member.discord_id === '245784383506743296' ? 'npc-card' : '' %>" data-user-id="<%= member.discord_id %>">
                        <% if (member.avatar_url) { %>
                            <img src="<%= member.avatar_url %>" alt="<%= member.username %>" class="staff-avatar">
                        <% } %>
                        <div class="staff-name"><%= member.username %></div>
                        <% if (member.discord_id === '245784383506743296') { %>
                            <div class="staff-role npc-badge">preprogrammed npc</div>
                        <% } %>
                    </div>
                <% }); %>
            </div>
        </div>
    </div>
    <% } %>

    <%- include('partials/footer') %>

    <script>
        // Scroll to top on page load and remove hash - runs once
        (function() {
            if ('scrollRestoration' in history) {
                history.scrollRestoration = 'manual';
            }

            // Remove hash from URL without triggering reload
            if (window.location.hash) {
                history.replaceState(null, null, window.location.pathname + window.location.search);
            }

            // Scroll to top smoothly
            window.scrollTo({ top: 0, behavior: 'instant' });
        })();

        // NPC Card explosion effect
        document.addEventListener('DOMContentLoaded', function() {
            const npcCard = document.querySelector('.npc-card[data-user-id="245784383506743296"]');

            if (npcCard) {
                npcCard.addEventListener('click', function(e) {
                    // Prevent multiple clicks during animation
                    if (this.classList.contains('exploding')) return;

                    // Shake animation first
                    this.classList.add('shaking');

                    setTimeout(() => {
                        this.classList.remove('shaking');

                        // Create explosion particles
                        const rect = this.getBoundingClientRect();
                        const centerX = rect.left + rect.width / 2;
                        const centerY = rect.top + rect.height / 2;

                        // Create 20 particles
                        for (let i = 0; i < 20; i++) {
                            const particle = document.createElement('div');
                            particle.className = 'explosion-particle';

                            // Random direction
                            const angle = (Math.PI * 2 * i) / 20;
                            const velocity = 100 + Math.random() * 100;
                            const tx = Math.cos(angle) * velocity;
                            const ty = Math.sin(angle) * velocity;

                            particle.style.left = rect.left + rect.width / 2 + 'px';
                            particle.style.top = rect.top + rect.height / 2 + 'px';
                            particle.style.setProperty('--tx', tx + 'px');
                            particle.style.setProperty('--ty', ty + 'px');

                            document.body.appendChild(particle);

                            // Remove particle after animation
                            setTimeout(() => particle.remove(), 800);
                        }

                        // Explode the card
                        this.classList.add('exploding');

                        // CHAOS MODE - Mess up the entire page!
                        setTimeout(() => {
                            document.body.classList.add('chaos-mode');

                            // Get all major elements to throw around
                            const chaosElements = document.querySelectorAll('.stat-card, .feature-card, .staff-card, .leaderboard-item, .faq-item, .btn, .hero-title, .section-title, .activity-item');

                            chaosElements.forEach((element, index) => {
                                // Random transformations
                                const randomX = (Math.random() - 0.5) * 1000;
                                const randomY = (Math.random() - 0.5) * 1000;
                                const randomRotate = (Math.random() - 0.5) * 720;
                                const randomScale = 0.5 + Math.random() * 1.5;

                                element.style.transition = 'all 1s ease-out';
                                element.style.transform = `translate(${randomX}px, ${randomY}px) rotate(${randomRotate}deg) scale(${randomScale})`;
                                element.style.opacity = Math.random() * 0.5 + 0.5;
                                element.style.zIndex = Math.floor(Math.random() * 1000);
                            });

                            // Mess up the hero section
                            const hero = document.querySelector('.hero');
                            if (hero) {
                                hero.style.transition = 'all 1s ease-out';
                                hero.style.transform = 'rotate(' + (Math.random() - 0.5) * 45 + 'deg) scale(0.8)';
                            }

                            // Spin some sections
                            const sections = document.querySelectorAll('.stats-section, .about-section, .activity-section, .leaderboard-section');
                            sections.forEach(section => {
                                section.style.transition = 'all 1.5s ease-out';
                                section.style.transform = 'rotate(' + (Math.random() - 0.5) * 360 + 'deg)';
                            });

                            // Reset everything after 10 seconds
                            setTimeout(() => {
                                // Reload the page for a clean reset
                                location.reload();
                            }, 10000);

                        }, 600); // Start chaos right after card explosion

                    }, 500); // Wait for shake to finish
                });
            }

            // CRAZY PARTY MODE for user profile (576244740199284779)
            const userCard = document.querySelector('.staff-card[data-user-id="576244740199284779"]');
            if (userCard) {
                userCard.style.cursor = 'pointer';
                userCard.addEventListener('click', function() {
                    if (document.body.classList.contains('party-mode')) return;

                    // ACTIVATE PARTY MODE!!!
                    document.body.classList.add('party-mode');

                    // Change background to rainbow disco
                    document.body.style.animation = 'rainbow-bg 0.5s linear infinite';

                    // Get ALL elements
                    const allElements = document.querySelectorAll('*:not(script):not(style)');

                    allElements.forEach((el, i) => {
                        setTimeout(() => {
                            el.style.animation = 'party-spin 1s ease-in-out infinite, party-bounce 0.5s ease-in-out infinite';
                            el.style.filter = `hue-rotate(${Math.random() * 360}deg) saturate(3)`;
                        }, i * 2);
                    });

                    // Spawn confetti everywhere
                    for (let i = 0; i < 200; i++) {
                        setTimeout(() => {
                            const confetti = document.createElement('div');
                            confetti.className = 'confetti';
                            confetti.style.left = Math.random() * 100 + 'vw';
                            confetti.style.background = `hsl(${Math.random() * 360}, 100%, 50%)`;
                            confetti.style.animationDelay = Math.random() * 2 + 's';
                            document.body.appendChild(confetti);
                            setTimeout(() => confetti.remove(), 5000);
                        }, i * 20);
                    }

                    // Make everything grow and shrink randomly
                    const cards = document.querySelectorAll('.stat-card, .feature-card, .staff-card, .faq-item, .leaderboard-item');
                    cards.forEach(card => {
                        card.style.animation = 'mega-bounce 0.3s ease-in-out infinite, rainbow-border 1s linear infinite';
                    });

                    // Spin the entire page
                    const sections = document.querySelectorAll('.hero, .stats-section, .about-section, .activity-section, .leaderboard-section, .faq-section, .staff-section');
                    sections.forEach(section => {
                        section.style.animation = 'party-wobble 0.5s ease-in-out infinite';
                    });

                    // Add party text
                    const partyText = document.createElement('div');
                    partyText.innerHTML = '🎉 PARTY MODE ACTIVATED! 🎊';
                    partyText.style.cssText = `
                        position: fixed;
                        top: 50%;
                        left: 50%;
                        transform: translate(-50%, -50%);
                        font-size: 60px;
                        font-weight: bold;
                        z-index: 99999;
                        color: white;
                        text-shadow: 0 0 20px #ff00ff, 0 0 40px #00ffff;
                        animation: party-pulse 0.5s ease-in-out infinite;
                        pointer-events: none;
                    `;
                    document.body.appendChild(partyText);

                    // Reset after 10 seconds
                    setTimeout(() => {
                        location.reload();
                    }, 10000);
                });
            }

            // CRAZY PARTY MODE for CUB's card!
            const cubCards = document.querySelectorAll('.staff-card');
            cubCards.forEach(card => {
                const username = card.querySelector('.staff-name');
                if (username && username.textContent.trim().toLowerCase() === 'cub') {
                    card.style.cursor = 'pointer';
                    card.addEventListener('click', function() {
                        if (document.body.classList.contains('party-mode')) return;

                        // ACTIVATE PARTY MODE!!!
                        document.body.classList.add('party-mode');

                        // Change background to rainbow disco
                        document.body.style.animation = 'rainbow-bg 0.5s linear infinite';

                        // Get ALL elements
                        const allElements = document.querySelectorAll('*:not(script):not(style)');

                        allElements.forEach((el, i) => {
                            setTimeout(() => {
                                el.style.animation = 'party-spin 1s ease-in-out infinite, party-bounce 0.5s ease-in-out infinite';
                                el.style.filter = `hue-rotate(${Math.random() * 360}deg) saturate(3)`;
                            }, i * 2);
                        });

                        // Spawn confetti everywhere
                        for (let i = 0; i < 200; i++) {
                            setTimeout(() => {
                                const confetti = document.createElement('div');
                                confetti.className = 'confetti';
                                confetti.style.left = Math.random() * 100 + 'vw';
                                confetti.style.background = `hsl(${Math.random() * 360}, 100%, 50%)`;
                                confetti.style.animationDelay = Math.random() * 2 + 's';
                                document.body.appendChild(confetti);
                                setTimeout(() => confetti.remove(), 5000);
                            }, i * 20);
                        }

                        // Make everything grow and shrink randomly
                        const cards = document.querySelectorAll('.stat-card, .feature-card, .staff-card, .faq-item, .leaderboard-item');
                        cards.forEach(card => {
                            card.style.animation = 'mega-bounce 0.3s ease-in-out infinite, rainbow-border 1s linear infinite';
                        });

                        // Spin the entire page
                        const sections = document.querySelectorAll('.hero, .stats-section, .about-section, .activity-section, .leaderboard-section, .faq-section, .staff-section');
                        sections.forEach(section => {
                            section.style.animation = 'party-wobble 0.5s ease-in-out infinite';
                        });

                        // Add party text
                        const partyText = document.createElement('div');
                        partyText.innerHTML = '🎉 PARTY MODE ACTIVATED! 🎊';
                        partyText.style.cssText = `
                            position: fixed;
                            top: 50%;
                            left: 50%;
                            transform: translate(-50%, -50%);
                            font-size: 60px;
                            font-weight: bold;
                            z-index: 99999;
                            color: white;
                            text-shadow: 0 0 20px #ff00ff, 0 0 40px #00ffff;
                            animation: party-pulse 0.5s ease-in-out infinite;
                            pointer-events: none;
                        `;
                        document.body.appendChild(partyText);

                        // Reset after 10 seconds
                        setTimeout(() => {
                            location.reload();
                        }, 10000);
                    });
                }
            });

            // Zigzag scroll path (disabled for better UX)
            // The scroll path feature has been disabled to improve scrolling performance
            // and remove the distracting X marker
        });
    </script>

    <!-- Party Mode CSS -->
    <style>
        @keyframes rainbow-bg {
            0% { background: #ff0000; }
            16.67% { background: #ff7f00; }
            33.33% { background: #ffff00; }
            50% { background: #00ff00; }
            66.67% { background: #0000ff; }
            83.33% { background: #8b00ff; }
            100% { background: #ff0000; }
        }

        @keyframes party-spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        @keyframes party-bounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-20px); }
        }

        @keyframes mega-bounce {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.3); }
        }

        @keyframes rainbow-border {
            0% { border-color: #ff0000; box-shadow: 0 0 20px #ff0000; }
            16.67% { border-color: #ff7f00; box-shadow: 0 0 20px #ff7f00; }
            33.33% { border-color: #ffff00; box-shadow: 0 0 20px #ffff00; }
            50% { border-color: #00ff00; box-shadow: 0 0 20px #00ff00; }
            66.67% { border-color: #0000ff; box-shadow: 0 0 20px #0000ff; }
            83.33% { border-color: #8b00ff; box-shadow: 0 0 20px #8b00ff; }
            100% { border-color: #ff0000; box-shadow: 0 0 20px #ff0000; }
        }

        @keyframes party-wobble {
            0%, 100% { transform: rotate(0deg); }
            25% { transform: rotate(5deg); }
            75% { transform: rotate(-5deg); }
        }

        @keyframes party-pulse {
            0%, 100% { transform: translate(-50%, -50%) scale(1); opacity: 1; }
            50% { transform: translate(-50%, -50%) scale(1.2); opacity: 0.8; }
        }

        .confetti {
            position: fixed;
            width: 10px;
            height: 10px;
            top: -10px;
            z-index: 9999;
            animation: confetti-fall 5s linear;
        }

        @keyframes confetti-fall {
            to {
                transform: translateY(100vh) rotate(720deg);
            }
        }
    </style>

    <script src="/js/activity.js"></script>
</body>
</html>
